{
  "name": "pedestrian_viz",
  "version": "1.0.0",
  "homepage": "https://shyamksateesh.github.io/pedestrian_viz",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "preview": "cd app && npm run preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d app/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
